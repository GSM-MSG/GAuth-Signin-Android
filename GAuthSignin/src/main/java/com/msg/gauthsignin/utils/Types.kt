package com.msg.gauthsignin.utils

object Types {
    enum class ActionType {
        SIGNIN,
        SIGNUP,
        CONTINUE
    }

    enum class Style {
        DEFAULT,
        ROUNDED
    }

    enum class Colors {
        WHITE,
        COLORED,
        OUTLINE
    }
}