package com.msg.gauthsignin.component.utils

object Types {
    enum class ActionType {
        SIGNIN,
        SIGNUP,
        CONTINUE
    }

    enum class Style {
        DEFAULT,
        ROUNDED
    }

    enum class Colors {
        WHITE,
        COLORED,
        OUTLINE
    }
}